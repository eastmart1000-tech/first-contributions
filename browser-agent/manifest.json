{
  "manifest_version": 3,
  "name": "Claude Browser Agent",
  "version": "0.1.0",
  "description": "A side-panel helper that reads the current page, scrolls and fills in fields, and asks before anything high-stakes.",
  "permissions": ["sidePanel", "tabs", "scripting", "storage", "activeTab"],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "background.js" },
  "side_panel": { "default_path": "sidepanel.html" },
  "action": { "default_title": "Open Claude Browser Agent" }
}
